# Roland-Plan

Roland-Plan 是一个防遗忘型个人日程 / 任务管理 Web App。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 数据原则

普通一次性任务只保存在同一个 `tasks` 数组中。今日、本周、下周、日历和搜索都只是基于日期、标签、完成状态计算出来的不同视图。
